"use client";import {useState} from 'react';import {AppShell} from '@/components/AppShell';import {PageHead} from '@/components/UI';
export default function Uploads(){const [found,setFound]=useState(false);return <AppShell><PageHead title="Uploads" subtitle="Screenshot extraction always requires confirmation before saving."/><div className="grid two"><div className="glass card"><div className="drop"><div className="eyebrow">SCREENSHOT / CLIP</div><h2>Drop post-game evidence here</h2><p className="muted">Scoreboard, match history, stats screen, item build screenshot, or a short gameplay clip.</p><input aria-label="Upload match media" type="file" accept="image/png,image/jpeg,image/webp,video/mp4,video/webm" onChange={()=>setFound(true)}/></div></div><div className="glass card"><div className="eyebrow">MANUAL MATCH</div><h2>Enter the essentials</h2><div className="grid two"><label className="field">Champion<input className="input" placeholder="Kog&apos;Maw"/></label><label className="field">Result<select className="input"><option>Victory</option><option>Defeat</option></select></label><label className="field">Kills<input className="input" inputMode="numeric"/></label><label className="field">Deaths<input className="input" inputMode="numeric"/></label><label className="field">Assists<input className="input" inputMode="numeric"/></label><label className="field">CS<input className="input" inputMode="numeric"/></label><label className="field">Duration<input className="input" placeholder="32:10"/></label></div><button className="btn primary">SAVE &amp; ANALYSE</button></div></div>{found&&<div className="glass card" style={{marginTop:18}}><div className="eyebrow">WE FOUND · DEMO EXTRACTION</div><div className="grid two"><label className="field">Champion<input className="input" defaultValue="Kog'Maw"/></label><label className="field">KDA<input className="input" defaultValue="8 / 4 / 10"/></label><label className="field">CS<input className="input" defaultValue="221"/></label><label className="field">Duration<input className="input" defaultValue="32:10"/></label><label className="field">Result<select className="input" defaultValue="Victory"><option>Victory</option><option>Defeat</option></select></label></div><p className="muted">This MVP intentionally uses editable demo extraction unless a multimodal AI service is configured server-side.</p><button className="btn primary">CONFIRM DATA</button></div>}</AppShell>}
