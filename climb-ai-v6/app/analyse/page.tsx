'use client';
import {AppShell} from '@/components/AppShell';import {MatchCard,PageHead} from '@/components/UI';import Link from 'next/link';import {useAccount,matchesFor} from '@/components/AccountContext';
export default function Analyse(){const {active}=useAccount();const matches=matchesFor(active.id);return <AppShell><PageHead title="Analyse" subtitle={`${active.gameName}${active.tagline} · ${active.rank} · ${active.role}`} action={<Link href="/uploads" className="btn primary">UPLOAD MATCH</Link>}/><div className="glass card" style={{marginBottom:18}}><div className="eyebrow">ACCOUNT-SPECIFIC REVIEW</div><h2>League evidence first. Advice second.</h2><p className="muted">For ADC, reviews split lane economy from post-lane economy, track CS@10/15, gold/XP @15, item timing, death phase, KP, damage share and objective involvement when the source provides them. Metrics that are not available are marked unavailable rather than invented.</p></div><div className="grid two">{matches.map(m=><MatchCard key={m.id} match={m}/>)}</div></AppShell>}
