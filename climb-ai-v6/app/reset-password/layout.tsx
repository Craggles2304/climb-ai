import type {Metadata} from 'next';
export const metadata:Metadata={title:'Choose New Password',description:'Choose a new password for your OP CLIMB account.',robots:{index:false,follow:false}};
export default function ResetLayout({children}:{children:React.ReactNode}){return children}
