import type {Metadata} from 'next';
export const metadata:Metadata={title:'Support',description:'Get help with OP CLIMB, account access, privacy requests or technical issues.',alternates:{canonical:'/support'}};
export default function SupportLayout({children}:{children:React.ReactNode}){return children}
