'use client';
import {useState} from 'react';
import Link from 'next/link';
import {AppShell} from '@/components/AppShell';
import {PageHead} from '@/components/UI';
import {useAccount,matchesFor} from '@/components/AccountContext';
import {useLearningPlan} from '@/components/LearningPlanContext';
import {AnimatedBar} from '@/components/Motion';
import {TrackView} from '@/components/TrackView';
import {FirstRun} from '@/components/FirstRun';
import {LiveGameCard} from '@/components/LiveGameCard';
import {ErrorBoundary} from '@/components/ErrorState';
import {missionEvidence} from '@/lib/missionLoop';
import {coachingLevelFor} from '@/lib/coachingLevel';
import {track} from '@/lib/analytics';
import {IlpExplainability} from '@/components/IlpExplainability';
export default function Home(){const {active,isEmpty,profile}=useAccount();const matches=matchesFor(active.id);const {tasks}=useLearningPlan();const detail=coachingLevelFor(active.rank);const [feedback,setFeedback]=useState<'yes'|'no'|null>(null);const activeTasks=tasks.filter(t=>t.status!=='MASTERED'&&t.status!=='PAUSED').slice(0,5);const leadTask=activeTasks[0];const last=matches[0];const recent=matches.slice(0,3);const leadEvidence=leadTask?missionEvidence(leadTask,last):null;const goodGames=leadTask?.successfulGames||0;const needed=leadTask?.masteryRequired||3;const leaveFeedback=(value:'yes'|'no')=>{if(!leadTask||feedback)return;setFeedback(value);track('feedback_given',{surface:'home_focus',helpful:value==='yes',rank:active.rank,category:leadTask.category,taskId:leadTask.id})};if(isEmpty)return <AppShell><TrackView event="dashboard_view" props={{state:'empty'}}/><PageHead title="Home" subtitle={`${active.gameName}${active.tagline} · ${active.rank} · ${active.role}`}/>{profile&&<ErrorBoundary label="live_game" compact><LiveGameCard gameName={profile.gameName} tagline={profile.tagline} region={profile.region} task={leadTask}/></ErrorBoundary>}<FirstRun task={leadTask} gameName={active.gameName}/></AppShell>;return <AppShell><TrackView event="dashboard_view"/><PageHead title="Home" subtitle={`${active.gameName}${active.tagline} · ${active.rank} · ${active.role}`}/><div className="v7-stack">{profile&&<ErrorBoundary label="live_game" compact><LiveGameCard gameName={profile.gameName} tagline={profile.tagline} region={profile.region} task={leadTask}/></ErrorBoundary>}<section className="hq-command"><div className="hq-top"><span className="v7-badge engine">{detail.tier} COACH</span><span className="v7-badge">THIS IS WHAT YOU'RE FIXING</span></div><h2>{leadTask?.title||'Play one tracked game'}</h2><p>{leadTask?.why||'Give OP CLIMB one proper game and it will find the first repeat worth fixing.'}</p><div className="hq-rules"><div><span className="label">NEXT GAME</span><b>{leadTask?.gameRule||'Play normally. We need a real game before giving you a rule.'}</b></div>{detail.depth>=3&&leadTask?.target&&<div><span className="label">YOU'VE GOT IT WHEN</span><b>{leadTask.target}</b></div>}</div>{leadTask&&<div className="hq-progress"><AnimatedBar value={leadTask.progress||0} delay={180}/><span>{goodGames}/{needed} CLEAN GAMES · {leadTask.progress||0}%</span></div>}{leadTask&&<IlpExplainability task={leadTask} compact/>}{leadTask&&leadEvidence&&detail.depth>=2&&<div className="rep" style={{marginTop:14}}><div className="eyebrow">WHAT WE SAW</div><b className="rep-headline">{leadEvidence.available?'This came from your game.':'We need more proof.'}</b><p className="rep-detail">{leadEvidence.reason}</p></div>}<div className="hero-actions"><Link className="btn primary" href="/session">START CLIMB SESSION →</Link><Link className="btn secondary" href="/coach">ASK COACH</Link></div>{leadTask&&<div style={{display:'flex',gap:9,alignItems:'center',flexWrap:'wrap',marginTop:16,paddingTop:14,borderTop:'1px solid rgba(255,255,255,.08)'}}><span className="muted" style={{fontSize:11}}>Did this focus actually help?</span>{!feedback?<><button className="btn secondary" style={{padding:'6px 9px',fontSize:9}} onClick={()=>leaveFeedback('yes')}>YES</button><button className="btn secondary" style={{padding:'6px 9px',fontSize:9}} onClick={()=>leaveFeedback('no')}>NOT REALLY</button></>:<span style={{fontSize:11,fontWeight:800}}>{feedback==='yes'?'Good — keep that one job for your next game.':'Thanks. Tell Coach what feels off and we’ll use that feedback.'}</span>}{feedback==='no'&&<Link className="text-link" href="/coach" style={{fontSize:10}}>TELL COACH →</Link>}</div>}</section></div>{activeTasks.length>1&&<section className="v7-section"><div className="v7-section-head"><div><div className="eyebrow">IN THE BACKGROUND</div><h2>You do not need to fix five things at once.</h2><p className="muted">These are being watched quietly. Your first focus stays first until the evidence says otherwise.</p></div><Link className="v7-link" href="/ilp">SEE YOUR PLAN →</Link></div><div className="hq-behaviours">{activeTasks.slice(1).map((task,index)=><Link key={task.id} className="glass hq-behaviour" href="/ilp"><div className="hq-bfoot"><span className="dc-index">0{index+2}</span><span className="v7-badge">WATCHING</span></div><h3>{task.title}</h3>{detail.depth>=4&&<><AnimatedBar value={task.progress||0} delay={index*60}/><div className="hq-bfoot"><span>{task.progress||0}%</span><span>{task.successfulGames||0}/{task.masteryRequired||3} CLEAN GAMES</span></div></>}</Link>)}</div></section>}<section className="v7-section"><div className="v7-section-head"><div><div className="eyebrow">RECENT GAMES</div><h2>What happened. What matters next.</h2></div><Link className="v7-link" href="/analyse">ALL GAMES →</Link></div>{recent.length?<div className="grid three">{recent.map(match=><Link href={`/analyse/${encodeURIComponent(match.id)}`} key={match.id} className="glass card" style={{display:'grid',gap:8}}><div style={{display:'flex',justifyContent:'space-between',gap:12,alignItems:'center'}}><span className="label">{match.champion}</span><strong className={match.result==='WIN'?'success':'danger'}>{match.result}</strong></div><h3 style={{margin:0}}>{match.kills}/{match.deaths}/{match.assists}</h3><small className="muted">Open the review →</small></Link>)}</div>:<div className="glass card"><b>No tracked games yet.</b><p className="muted">Play with the Companion or add a game and this page starts becoming personal.</p><Link className="btn primary" href="/session">START CLIMB SESSION</Link></div>}</section></AppShell>}
