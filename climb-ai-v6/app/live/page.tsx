'use client';
import {AppShell} from '@/components/AppShell';
import {PageHead} from '@/components/UI';
import {useAccount} from '@/components/AccountContext';
import {useLearningPlan} from '@/components/LearningPlanContext';
export default function Live(){const {active}=useAccount();const {tasks}=useLearningPlan();const mission=tasks.find(t=>t.status!=='MASTERED'&&t.status!=='PAUSED');return <AppShell><PageHead title="Live Companion" subtitle="Phase 2 foundation · follow permitted local game telemetry without turning CLIMB//AI into an automated shotcaller."/><section className="wow-grid"><div className="glass wow-main"><div className="eyebrow">COMPANION STATUS</div><h2>NOT CONNECTED</h2><p>The browser cannot read League's local Live Client Data API directly. A small Windows companion runs on the same PC as League, reads permitted local telemetry, and sends selected snapshots to your CLIMB//AI account.</p><div className="mission-command"><span>PRE-GAME CUE</span><b>{mission?.gameRule||'Open your ILP before queueing.'}</b></div><button className="btn primary" disabled>WINDOWS COMPANION · PHASE 2</button></div><div className="dashboard-stack"><div className="glass stat-feature"><span>ACTIVE ACCOUNT</span><strong>{active.gameName}{active.tagline}</strong><small>{active.region} · {active.role}</small></div><div className="glass stat-feature"><span>DURING GAME</span><strong>SILENT TELEMETRY</strong><small>Game time, exposed stats and events only</small></div><div className="glass stat-feature"><span>AFTER GAME</span><strong>ADAPT ILP</strong><small>Update task evidence and mission progress</small></div></div></section><div className="grid three dash-section"><div className="glass card"><div className="eyebrow">WHAT LIVE MODE CAN DO</div><h3>Follow permitted signals</h3><p className="muted">Track exposed local game state, keep your single ILP cue visible, timestamp events and create richer post-game evidence.</p></div><div className="glass card"><div className="eyebrow">WHAT IT SHOULD NOT DO</div><h3>No automated shotcalling</h3><p className="muted">No hidden-information inference, no “go Baron now”, no automated tactical decisions that play the match for you.</p></div><div className="glass card"><div className="eyebrow">WHY IT MATTERS</div><h3>Better ILP evidence</h3><p className="muted">The value is a stronger post-game learning loop: mission result → evidence → adaptive task progress → next cue.</p></div></div></AppShell>}
