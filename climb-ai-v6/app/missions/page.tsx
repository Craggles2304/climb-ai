'use client';
import {AppShell} from '@/components/AppShell';import {MissionCard,PageHead} from '@/components/UI';import {useAccount,missionFor} from '@/components/AccountContext';
export default function Missions(){const {active}=useAccount();const mission=missionFor(active.id);return <AppShell><PageHead title="Mission Lab" subtitle={`${active.gameName}${active.tagline} · Discover → Practise → Repeat → Master.`}/><MissionCard mission={mission}/><div className="glass card" style={{marginTop:18}}><div className="eyebrow">WHY THIS IS A LEAGUE MISSION</div><h2>{active.role==='ADC'?'Lane assignment after first towers':'Objective setup tempo'}</h2><p className="muted">{active.role==='ADC'?'The mission does not tell you to “farm more”. It isolates the decision window where ADCs often bleed economy: post-15 recalls, side-wave assignment and grouping before an objective is actually contestable.':'The mission does not tell you to “get objectives”. It tests whether your recall/invade timing lets you arrive with HP, items and map position before neutral objectives spawn.'}</p></div></AppShell>}
