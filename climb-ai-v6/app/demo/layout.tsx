import type {Metadata} from 'next';
export const metadata:Metadata={title:'Interactive Demo',description:'See a real OP CLIMB coaching flow before creating an account: match grade, Fix Ladder, map tempo and next-game rules.',alternates:{canonical:'/demo'}};
export default function DemoLayout({children}:{children:React.ReactNode}){return children}
