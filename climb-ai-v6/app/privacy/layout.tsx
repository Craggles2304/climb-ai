import type {Metadata} from 'next';
export const metadata:Metadata={title:'Privacy Policy',description:'How OP CLIMB handles account, match, tracker and coaching data.',alternates:{canonical:'/privacy'}};
export default function PrivacyLayout({children}:{children:React.ReactNode}){return children}
