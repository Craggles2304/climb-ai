import type {Metadata} from 'next';
export const metadata:Metadata={title:'Terms of Service',description:'Terms for using OP CLIMB League of Legends coaching and match-analysis features.',alternates:{canonical:'/terms'}};
export default function TermsLayout({children}:{children:React.ReactNode}){return children}
