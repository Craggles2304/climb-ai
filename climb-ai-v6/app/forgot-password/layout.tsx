import type {Metadata} from 'next';
export const metadata:Metadata={title:'Reset Password',description:'Recover access to your OP CLIMB account.',robots:{index:false,follow:false}};
export default function ForgotLayout({children}:{children:React.ReactNode}){return children}
