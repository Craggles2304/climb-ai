'use client';
import Link from 'next/link';
import {useAccount} from './AccountContext';
const desk=[['Dashboard','/dashboard'],['Analyse','/analyse'],['Missions','/missions'],['Champions','/champions'],['Progress','/progress'],['Coach','/coach'],['Uploads','/uploads'],['Subscription','/pricing'],['Accounts','/account'],['Settings','/settings']];
const mobile=[['Home','/dashboard'],['Analyse','/analyse'],['Mission','/missions'],['Coach','/coach'],['Profile','/account']];
export function AppShell({children}:{children:React.ReactNode}){const {accounts,active,setActive}=useAccount();return <div className="app-layout"><aside className="sidebar"><Link href="/" className="logo">CLIMB<span>//AI</span></Link><div className="account-switch"><div className="label">ACTIVE RIOT ACCOUNT</div><select aria-label="Active Riot account" value={active.id} onChange={e=>setActive(e.target.value)}>{accounts.map(a=><option key={a.id} value={a.id}>{a.gameName}{a.tagline} · {a.region}</option>)}</select><div><strong>{active.rank}</strong><span>{active.role} · {active.label}</span></div></div><nav className="nav">{desk.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}</nav><div style={{position:'absolute',bottom:24,left:18,right:18}} className="glass card"><div className="eyebrow">DEMO DATA</div><p className="muted" style={{fontSize:13,marginBottom:0}}>Multi-account structure is working locally. Riot production sync remains feature-flagged until credentials/approval are connected.</p></div></aside><main className="app-main">{children}</main><nav className="mobile-nav"><div>{mobile.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}</div></nav></div>}
