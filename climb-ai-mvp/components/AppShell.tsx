import Link from 'next/link';
const desk=[['Dashboard','/dashboard'],['Analyse','/analyse'],['Missions','/missions'],['Champions','/champions'],['Progress','/progress'],['Coach','/coach'],['Uploads','/uploads'],['Subscription','/pricing'],['Settings','/settings']];
const mobile=[['Home','/dashboard'],['Analyse','/analyse'],['Mission','/missions'],['Coach','/coach'],['Profile','/account']];
export function AppShell({children}:{children:React.ReactNode}){return <div className="app-layout"><aside className="sidebar"><Link href="/" className="logo">CLIMB<span>//AI</span></Link><nav className="nav">{desk.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}</nav><div style={{position:'absolute',bottom:24,left:18,right:18}} className="glass card"><div className="eyebrow">DEMO MODE</div><p className="muted" style={{fontSize:13,marginBottom:0}}>Riot sync is feature-flagged off. Upload and demo analysis remain fully usable.</p></div></aside><main className="app-main">{children}</main><nav className="mobile-nav"><div>{mobile.map(([n,h])=><Link key={h} href={h}>{n}</Link>)}</div></nav></div>}