"use client";import {useState} from 'react';import {AppShell} from '@/components/AppShell';import {PageHead} from '@/components/UI';
export default function Matchups(){const [mine,setMine]=useState("Kog'Maw");const [opp,setOpp]=useState('Draven');return <AppShell><PageHead title="Matchup Assistant" subtitle="Simple lane plans first; deeper concepts scale with rank."/><div className="glass card form"><div className="field"><label>My champion</label><input className="input" value={mine} onChange={e=>setMine(e.target.value)}/></div><div className="field"><label>Opponent</label><input className="input" value={opp} onChange={e=>setOpp(e.target.value)}/></div><div className="field"><label>Role</label><select className="input"><option>ADC</option><option>Top</option><option>Jungle</option><option>Mid</option><option>Support</option></select></div></div><div className="glass card" style={{marginTop:18}}><div className="eyebrow">{mine.toUpperCase()} VS {opp.toUpperCase()}</div><h2>DIFFICULTY · Hard</h2><div className="grid three"><div><div className="label">LEVELS 1–3</div><p>Respect early all-in.</p></div><div><div className="label">FIRST GOAL</div><p>Preserve health and farm.</p></div><div><div className="label">KEY THREAT</div><p>Extended early trade.</p></div></div><p className="muted">Curated matchup guidance should be connected to approved static/official data before public launch. Unsupported certainty is intentionally avoided in the MVP.</p></div></AppShell>}
