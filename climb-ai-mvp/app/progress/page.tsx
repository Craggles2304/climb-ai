"use client";import {AppShell} from '@/components/AppShell';import {PageHead,MetricCard} from '@/components/UI';import {demoMatches} from '@/data/demo';import {LineChart,Line,CartesianGrid,XAxis,YAxis,Tooltip,ResponsiveContainer} from 'recharts';
const data=[...demoMatches].reverse().map((m,i)=>({game:i+1,cs:m.metrics.csPerMin,deaths:m.deaths,score:58+i*2+(i>5?2:0)}));
export default function Progress(){return <AppShell><PageHead title="Progress" subtitle="Only the trends that should change what you do next."/><div style={{display:'flex',gap:8,flexWrap:'wrap',marginBottom:18}}>{['7 Days','30 Days','90 Days','Season'].map(x=><button className="btn secondary" key={x}>{x}</button>)}</div><div className="grid three"><MetricCard label="CS/MIN" value="5.7" detail="+0.5 vs prior period"/><MetricCard label="DEATHS" value="5.2" detail="-0.7 vs prior period"/><MetricCard label="MISSION COMPLETION" value="68%" detail="Last 30 days"/></div><div className="glass card" style={{height:360,marginTop:18}}><div className="eyebrow">CLIMB SCORE TREND</div><div style={{height:290,marginTop:16}}><ResponsiveContainer width="100%" height="100%"><LineChart data={data}><CartesianGrid stroke="rgba(255,255,255,.06)"/><XAxis dataKey="game" stroke="#98A2B3"/><YAxis domain={[40,100]} stroke="#98A2B3"/><Tooltip contentStyle={{background:'#101522',border:'1px solid rgba(255,255,255,.1)'}}/><Line dataKey="score" stroke="#23D5E8" strokeWidth={3} dot={false}/></LineChart></ResponsiveContainer></div></div><div className="glass card" style={{marginTop:18}}><div className="eyebrow">WHAT IT MEANS</div><h2>Your consistency is improving faster than your economy.</h2><p className="muted">WHY IT MATTERS: fewer volatile games makes your next leak easier to isolate. WHAT TO DO: keep the resource discipline mission active rather than switching focus.</p></div></AppShell>}
