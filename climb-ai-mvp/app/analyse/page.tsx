import {AppShell} from '@/components/AppShell';import {MatchCard,PageHead} from '@/components/UI';import {demoMatches} from '@/data/demo';import Link from 'next/link';
export default function Analyse(){return <AppShell><PageHead title="Analyse" subtitle="Import a match, upload evidence, or analyse a recent game." action={<Link href="/uploads" className="btn primary">UPLOAD MATCH</Link>}/><div className="glass card" style={{marginBottom:18}}><div className="eyebrow">AUTOMATIC MATCH SYNC</div><h2>Coming shortly.</h2><p className="muted">The Riot integration is isolated behind a feature flag. Manual and screenshot data already use the same internal match model.</p><Link className="btn secondary" href="/uploads">UPLOAD MATCH INSTEAD</Link></div><div className="grid two">{demoMatches.slice(0,6).map(m=><MatchCard key={m.id} match={m}/>)}</div></AppShell>}
