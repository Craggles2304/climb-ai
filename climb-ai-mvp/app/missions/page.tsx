import {AppShell} from '@/components/AppShell';import {MissionCard,PageHead} from '@/components/UI';import {demoMission} from '@/data/demo';
export default function Missions(){return <AppShell><PageHead title="Missions" subtitle="Discover → Practise → Repeat → Master → Next focus."/><MissionCard mission={demoMission}/><div className="glass card" style={{marginTop:18}}><div className="eyebrow">COMPLETED</div><h2>EARLY SURVIVAL</h2><div className="grid three"><div><div className="label">DEATHS BEFORE 10</div><div className="metric">1.4 → 0.6</div></div><div><div className="label">REWARD</div><div className="metric">+12 XP</div></div><div><div className="label">STATUS</div><div className="metric success">MASTERED</div></div></div></div></AppShell>}
