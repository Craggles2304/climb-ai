"use client";import {useState} from 'react';import {AppShell} from '@/components/AppShell';import {PageHead} from '@/components/UI';
const canned=(q:string)=>q.toLowerCase().includes('kog')?"Across your tracked Kog'Maw games, the clearest current signal is still economy after lane. FACT: your recent CS/min is below your active mission target. INTERPRETATION: grouping too early may be reducing item timing consistency. SUGGESTION: keep the resource mission for three relevant games before adding another concept.":"Your active mission is Resource Discipline. I would keep one focus: after recall, check both side lanes before defaulting mid. I do not have enough reliable data to claim a more specific cause from this question alone.";
export default function Coach(){const [messages,setMessages]=useState([{who:'ai',text:'Ask me about your tracked games, active mission, champion patterns or what to focus on next.'}]);const [q,setQ]=useState('');function send(){if(!q.trim())return;const t=q;setMessages(m=>[...m,{who:'user',text:t},{who:'ai',text:canned(t)}]);setQ('')}return <AppShell><PageHead title="Coach" subtitle="Answers grounded in authorised app data. Unknown data stays unknown."/><div className="glass card"><div className="chat">{messages.map((m,i)=><div key={i} className={`bubble ${m.who}`}>{m.text}</div>)}</div><div style={{display:'flex',gap:10,marginTop:18}}><input aria-label="Ask Coach" className="input" value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&send()} placeholder="Why do I fall behind after lane?"/><button className="btn primary" onClick={send}>SEND</button></div></div><div style={{display:'flex',gap:8,flexWrap:'wrap',marginTop:14}}>{['Why am I losing on Kog\'Maw?','What should I focus on this week?','How did my last five games compare?'].map(x=><button key={x} className="btn secondary" onClick={()=>setQ(x)}>{x}</button>)}</div></AppShell>}
