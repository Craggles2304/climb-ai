import {NextResponse} from 'next/server';import type {NextRequest} from 'next/server';export function middleware(req:NextRequest){if(req.nextUrl.pathname.startsWith('/admin')&&process.env.NEXT_PUBLIC_DEMO_MODE!=='true'){const isAdmin=req.cookies.get('climb_admin')?.value==='1';if(!isAdmin)return NextResponse.redirect(new URL('/dashboard',req.url));}return NextResponse.next()}export const config={matcher:['/admin/:path*']};